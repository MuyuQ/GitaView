export function SafetySettings() {
  return (
    <section className="settings-card">
      <h3>安全操作</h3>
      <p className="settings-message">Pull 和 Push 始终需要二次确认，避免误操作修改工作区或远端分支。</p>
    </section>
  );
}
